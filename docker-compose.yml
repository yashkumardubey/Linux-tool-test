services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  agent:
    build: ./agent
    environment:
      - CONTROLLER_URL=http://backend:8000
    depends_on:
      - backend
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
